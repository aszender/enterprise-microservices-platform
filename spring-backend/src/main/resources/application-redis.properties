# Enable Redis-backed caching only when the 'redis' profile is active.
# This lets you keep running locally/tests without having Redis installed.

spring.cache.type=redis

# Redis connection (defaults)
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Cache behavior
# You can tune TTLs per-cache later via a CacheManager bean if needed.
